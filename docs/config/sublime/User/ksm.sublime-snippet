<snippet>
	<content><![CDATA[
int ksm(int a,int b) {
	int ans=1;
	while(b) {
		if(b&1) ans=1ll*a*ans%mod;
		a=1ll*a*a%mod;
		b>>=1;
	} return ans;
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>ksm</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope>
</snippet>
