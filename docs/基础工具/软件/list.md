```text
$ sudo pacman -S tree a b c d e f g
# 微信输入法
# marktext markdown开源软件
# cloc 统计代码行
# postman,apipost : api测试软件
# net-tools : netstat等工具
# scrcpy : GitHub开源安卓设备投屏软件，多平台
# openssh-server
# openjdk-18-jdk : sudo apt-cache search openjdk
# nodejs、npm ：
# mysql-server
# tree : 查看目录结构
# python-pip : Python
# neovim,vim,vim-gtk,vim-gnome
# screenfetch、neofetch : 查看系统信息
# screenkey : 显示按下的键
# figlet,toilet : 艺术字生成，可以用作logo .showfigfonts。-f指定字体，
    toilet -f mono12 -F gay a girl
    watch -n1 "date '+%D%n%T'|figlet -k"    显示时钟
    while true; do echo "$(date '+%D %T' | toilet -f term -F border --gay)"; sleep 1; done
# iputils : 允许用户使用 ping 命令 
# vscode
# sougoushuru 直接安装，还要安装fcitx，设置里面换用fcitx框架即可
# sublime text
# texlive-full : LaTex的编译器，编辑器可选下一行和vscode
# texstudio or texmaker : 关于LaTeX
# git 版本管理
# wps 金山
# vlc 视频播放器 
# rar
# p7zip-full
# QQ
# geogebra 数学
# obs-studio 录屏等
# g++、gcc
# cmake
# jetbrains toolbox
# docker
# gnome-tweaks gnome小工具,可以管理开机自启动，安装后叫“优化”,小小的细节美化alt+f2搜索打开
# sudo snap install pycharm-community --classic
# sl : 能开火车 man sl查看帮助
# fortune : 输出一句话，有笑话，名言什么的 (还有唐诗宋词sudo apt-get install fortunes-zh)
# cowsay : 用ASCII字符打印牛，羊等动物 cowsay "I am not a cow, hahaha" 或者 cowthink。 cowsay -l  查看其它动物的名字，然后-f跟上动物名，如$cowsay -f tux "哈哈" 或者 fortune | cowsay
# yes : 输出无穷无尽的字符,按ctrl+c结束
# cal: (ncal)cal 9 1752 打印日历
# factor : 分解因数
# aptitude : 小玩具 aptitude moo,aptitude -v moo,aptitude -vv moo,aptitude -vvv moo,aptitude -vvvv moo,aptitude -vvvvv moo,aptitude -vvvvvv moo,aptitude -vvvvvvv moo
# :(){ :|: & };:  :  Bash fork 炸弹。它会反复启动新的 Bash shell
# viu :Viu 是用 Rust 编程语言编写的，因此可以使用 Cargo 包管理器下载。 cargo install viu
